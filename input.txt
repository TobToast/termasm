/*
 * A program that generates fibonacci numbers to test the TERM64 CPU.
 *
*/
fibonacci:
    mov 1 -> r0
    mov 0 -> r1
    mov 0 -> r2
    mov 0 -> r3
.loop:
    st r3 r2
    inc r3 -> r3
    add r0 r1 -> r2
    mov r1 -> r0
    mov r2 -> r1
    mov .loop -> pc